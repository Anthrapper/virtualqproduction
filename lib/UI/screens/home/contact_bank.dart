import 'package:flutter/material.dart';
import 'package:virtualq/UI/widgets/app_bar.dart';

class Contact extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: CustomAppBar('Contact Bank'),
      body: Container(),
    );
  }
}
